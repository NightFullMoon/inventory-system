using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShopPanel : InventoryInstance {
    private void Start()
    {
        storeItem(1);
        storeItem(2);
        storeItem(3);
    }

}
